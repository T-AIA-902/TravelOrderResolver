---
paths:
  - "src/nlp/**/*.py"
---

# NLP Module Rules

When making major structural changes to `src/nlp/` (adding/removing/renaming modules or subfolders), update the Architecture section in `.claude/CLAUDE.md` to reflect the new structure.
